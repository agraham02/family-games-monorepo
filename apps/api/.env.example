# API Environment Variables
# Copy this file to .env and fill in the values

# Server Configuration
PORT=4000
NODE_ENV=development

# Room Configuration
ROOM_EMPTY_TTL_SECONDS=300
RECONNECT_TIMEOUT_MINUTES=2

# Supabase Configuration (optional - falls back to code-defined games if not set)
# Get these from your Supabase project: Settings > API Keys
SUPABASE_URL=https://your-project.supabase.co

# Use ONE of the following (secret key is recommended):
# - New Secret key (sb_secret_...): Copy from "Publishable and secret API keys" tab
# - Legacy service_role key (eyJ...): Copy from "Legacy anon, service_role API keys" tab
SUPABASE_SECRET_KEY=your-secret-key-or-service-role-key
